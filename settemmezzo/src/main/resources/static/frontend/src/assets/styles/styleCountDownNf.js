import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  countDownContainer:{
    backgroundColor: 'purple',
    padding: 10,
    borderRadius: 50
  },
  countDown:{
    textAlign: 'center',
  }
})